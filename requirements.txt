# Sentinel + spectral H1/H2 reproduction stack.
# Pinned to the versions used to produce the results in PROJECT_GUIDE.md;
# the exact pins matter for the Qwen2.5 attention path inside transformers
# (the streaming extractor monkey-patches Qwen2DecoderLayer.forward).

# Core ML
torch==2.6.0
transformers==4.50.2
bitsandbytes>=0.43.0          # 4-bit reader (Qwen2.5-7B-Instruct)
huggingface_hub>=0.24.0       # model + dataset downloads

# Numerics
numpy==1.23.5
scipy==1.13.1
scikit-learn==1.3.0
networkx>=3.0                 # H1 sanity checks + spectral plumbing

# Sentence segmentation
nltk==3.9.1
spacy==3.8.3
spacy-legacy==3.0.12
spacy-loggers==1.0.5
spacy_pkuseg==1.0.0

# Datasets
datasets>=2.18.0              # SQuAD v2 loader (experiments/select_examples.py)

# Misc
joblib>=1.3.0                 # detector pkl loader (loaded on import even when unused)
tiktoken>=0.5.0               # token counting in QA reader path

# Test runner
pytest>=7.0
